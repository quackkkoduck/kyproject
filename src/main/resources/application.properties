spring.application.name=demo

# spring.security.filter-dispatcher-types=ASYNC, FORWARD, INCLUDE, REQUEST

# # 스프링 시큐리티 로그인 페이지 비활성화
# spring.security.form-login.enabled=false


# H2 데이터베이스 설정
# spring.datasource.url=jdbc:h2:mem:testdb
# 위 방식은 메모리에만 저장되며 파일로 저장안됨/테스트에 적합
spring.datasource.url=jdbc:h2:file:C:/Users/user2/test
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform: org.hibernate.dialect.H2Dialect

spring.jpa.hibernate.ddl-auto: create
# H2 콘솔 활성화
spring.h2.console.enabled=true

# H2 콘솔 경로 설정
spring.h2.console.path=/h2-console

# Hibernate 설정 (옵션)
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
